const colors = {
  mainBlue: "#3692ff",
};

const mediaSize = {
  tablet: "screen and (max-width: 1199px)",
  mobile: "screen and (max-width: 743px)",
};

const theme = {
  colors,
  mediaSize,
};

export default theme;
