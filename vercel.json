{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "buildCommand": "npm run build",  // 빌드 명령어
        "outputDirectory": "dist"        // 빌드 후 결과물
      }
    }
  ]
}
